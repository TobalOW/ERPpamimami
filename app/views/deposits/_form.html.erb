<%= form_with(model: deposit, local: true) do |form| %>
  <% if deposit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(deposit.errors.count, "error") %> prohibited this deposit from being saved:</h2>

      <ul>
      <% deposit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.text_field :order_id, id: :deposit_order_id %>
  </div>

  <div class="field">
    <%= form.label :total %>
    <%= form.number_field :total, id: :deposit_total %>
  </div>

  <div class="field">
    <%= form.label :method %>
    <%= form.text_field :method, id: :deposit_method %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
