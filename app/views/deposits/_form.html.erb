<%= simple_form_for @deposit do |f| %>
  <%= f.input :order_id, :as => :hidden %>
  <%= f.input :total, label: 'Monto abono' %>
  <%= f.input :method, label: 'Método de pago' %>
  <%= f.input :comment, label: 'Comentarios' %>
  <%= f.button :submit, value: 'Enviar', class: 'btn-success' %>
<% end %>
